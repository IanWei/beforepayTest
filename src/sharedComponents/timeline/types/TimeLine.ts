export interface TimeLineProps {
    active: number,
    prevActive: number,
    handleClick(index: number): void
}

export interface IconListProps {
    active: number,
    prevActive: number,
    handleClick(index: number): void
}
